import { Link } from "react-router-dom";
import "./styles";
import * as React from "react";

const View = () => {
    return (
        <div>
            <Link to='/'>Назад</Link>
        </div>
    );
};

export default View;
