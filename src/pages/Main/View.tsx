import * as React from "react";
import { Wrapper, Button } from "./styles";

const View = () => {
    return (
        <Wrapper>
            <Button to='/form'>Заполнить анкету</Button>
        </Wrapper>
    );
};

export default View;
